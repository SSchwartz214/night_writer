class Parser
  

  def help
    puts "Available commands: que count, queue clear,
          queue district, queue print, queue print by <attribute>, queue save
          to <filename.csv>, queue export html <filename.csv>,
          find <attribute> <criteria>"
  end


end
